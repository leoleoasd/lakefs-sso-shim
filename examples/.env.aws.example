# Copy to .env for docker-compose.aws.yml

# Region of your S3 bucket (and where this EC2 runs), e.g. us-east-1
AWS_REGION=us-east-1

# lakeFS encryption secret — random, stable, secret. Generate with: openssl rand -hex 24
LAKEFS_ENCRYPT_SECRET=change-me-openssl-rand-hex-24
